// Convertir las siguientes funciones a funciones flecha

// función convencional
function calcularArea (base, altura) {
    let area = (base * altura)/2;
    return area;
}

// función flecha



// función convencional
function mostrarEdad () {
    let info = {
        nombre: 'Diana',
        apellido: 'Torres',
        edad: 15
    }

    // completar función para entregar la edad de Diana

    // return ...
}

// función flecha